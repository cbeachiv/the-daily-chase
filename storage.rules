rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Mirror firestore.rules: each user can read/write only their own files.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
